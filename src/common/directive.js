import Vue from 'vue'
 
Vue.directive('permission', {
    bind: function(el, binding, vnode) {
    
        let hasPermission = false
      
 
        if (!hasPermission) {
            // el.style.display = 'none'
        }
    }
})
